# 隐私说明

Quicker Link 不提供账号系统，不包含广告、分析、崩溃上报或第三方统计 SDK。

## 本地保存的数据

App 会在设备本地保存：

- 电脑局域网 IPv4 和端口
- 用户创建的动作快捷项及其参数
- 已同步动作的名称、分组、顺序、图标描述和右键菜单快捷参数
- 用户选择“加密保存验证码”时的连接验证码
- “后台接收与连接”的开关状态
- “接收提示音”的开关状态
- 当前屏幕预览和正在传输的小文件临时副本

连接验证码使用 Android Keystore 中不可导出的 AES 密钥加密。为避免凭据或动作配置随系统迁移，App 关闭了 Android 自动备份，并在数据提取规则中对云备份和设备迁移显式排除共享偏好、文件、数据库、外部文件及设备保护存储域。

连接页面的最近记录只保存在当前 App 进程内，不写入磁盘。动作记录显示名称和目标，成功响应只保留简短结果，不保存动作目录、完整响应数据、剪贴板正文或电脑通知正文。电脑下发的最近一条文本会在当前进程内暂存，用于 Activity 重建后恢复传输编辑框；它同样不会写入磁盘，进程结束后即消失。

配套 Quicker 动作另有一个默认关闭的电脑本地“记录文本”选项。只有电脑端用户在配对窗口显式开启后，手机写入电脑剪贴板的正文才会保存在 `%LOCALAPPDATA%\QuickerLink\received-text-history.json`，最多 100 条且文件不超过 1 MiB；配对窗口提供查看和清空入口。这份可选历史由电脑上的配套动作管理，不会上传到本项目。

## 网络通信

App 的动作执行和目录同步只通过 `WSS` 连接局域网内的 Quicker WebSocket 服务，不提供明文 `WS` 模式。

自动发现仅探测当前 Wi-Fi 或以太网私有 IPv4 网段中的配置端口，探测请求不包含连接验证码。完整扫描只发现一个 WSS 候选时，App 才与该地址建立正常连接；配置了验证码时才发送验证码认证，留空时只接受 Quicker 的无验证码握手。发现多个候选时要求改用配对码或手动地址。

扫描配对码时，相机画面只在设备内存中交给 ZXing 解析，不保存照片，也不上传画面或二维码内容。

“传输”页面提供由用户主动发起的屏幕快照、当前有效快照左键点击、电脑剪贴板读取或写入、单文件传输，以及睡眠、关机和重启 Quicker 固定控制。点击模式开启时，App 会以约 1.2 秒间隔串行获取新快照，退出预览、页面隐藏或断线后停止；这不是视频录制。电脑控制每次都要求手机端再次确认；App 不发送任意拖动、命令行或通用键盘输入。用户在动作发送提示中明确使用“发送后按回车”时，只额外发送一个回车键。

电脑本机的 Quicker 动作可以向当前已认证的手机发送文本、通知或文件邀请。文本会优先写入手机剪贴板并保留在传输编辑框中；即使厂商系统阻止后台剪贴板写入，正文仍会保留在 App 当前进程内。通知通过 Android 系统通知显示；文件只在用户确认接收后下载到手机。普通远端调用不能借此指定电脑文件路径。Android 13 及以上的通知权限只影响电脑系统通知能否展示，不决定后台 WSS 连接是否保留，也不控制独立的接收提示音。

App 收到有效电脑文本、通知或文件邀请时，可以在本机播放一次轻短提示音；该设置默认开启，可由用户独立关闭。Android 通知权限只决定通知是否显示，不影响提示音开关。提示音不读取、录制或上传环境声音，并遵循手机静音、通知音量和系统对通知音频的静音控制。

App 只在用户操作传输页时读取或写入电脑剪贴板，不持续监听手机或电脑剪贴板。用户可以自行在 Quicker 中创建剪贴板变化触发动作并通过 `sendText:` 发送正文；这种变化检测发生在用户自己的电脑自动化中，App 只处理收到的文本。

手机不能远程浏览电脑目录，也不能指定 Windows 保存路径。手机主动选择“接收文件”时，必须由电脑端用户在系统文件选择窗口中确认；电脑本机自动化也可以指定一个本机路径发出文件邀请，但已连接手机不能远端提交该路径，而且仍需手机用户确认接收。发送到电脑的目录只能在 Quicker Link 配对窗口中由电脑端用户选择，默认遵循 Windows 实际“下载 / Quicker Link”位置。单个文件限制为 64 MiB，传输中使用临时文件，校验完成后才发布到最终目录。

App 不会把动作、参数、验证码、屏幕、文件或剪贴板内容发送给项目维护者或其他云端服务。

“后台接收与连接”默认开启并可由用户关闭。App 使用 Android `connectedDevice` 前台服务，在正常锁屏、切换应用或 Activity 重建后保留当前局域网连接。电脑文本会优先写入手机剪贴板，并始终在当前进程中保留供传输编辑区恢复；授予通知权限后会发出不含正文的提示并展示电脑通知；文件邀请会暂存在内存，允许通知时也会提示用户打开 App 确认接收。拒绝通知权限不会关闭后台 WSS 服务。App 不注册开机启动；服务被主动停止、App 被强制停止或进程被系统结束后，不保证仍能即时接收电脑消息。

短信监控/验证码转发与手机系统级代理目前均未实现。本版不申请读取或接收短信的权限，不访问、监控或转发短信及短信验证码；也不声明或使用 `VpnService`，不修改手机系统代理，不会把手机其他应用的流量转发到电脑或 Clash。

展示动作网格时，App 可能从 Quicker 官方静态文件域名 `files.getquicker.net` 加载动作设置的 PNG 图标。客户端严格限制为该域名下的图标和系统图片路径，不接受任意第三方图标地址；请求不会附带连接验证码、动作参数、剪贴板内容或局域网地址。Quicker 字体图标由电脑端在本地转换为 PNG 数据，不产生图标网络请求。

只有用户在“关于”页面主动点击“手动检查更新”时，App 才会先读取项目 GitHub Pages 上的更新索引；索引不可用时才回退查询一次 GitHub Releases 公开接口。发现新版本后，只有用户继续点击“下载并安装”，App 才会从对应 Release 下载 APK 与 SHA-256 文件，在本地完成校验后交给 Android 系统安装器。请求不包含设备标识、Quicker 数据或连接信息；App 不会在后台自动发起检查、下载或安装。点击项目、作者、支持、反馈或配套动作入口时，将由系统浏览器打开对应网页。

## 删除数据

删除 App 会一并删除其本地配置、增强功能开关、动作快捷项、缓存中的屏幕预览、传输临时文件和加密密钥。用户明确保存到手机系统“下载 / Quicker Link”或电脑端接收目录的文件不会随卸载自动删除。电脑端用户另行开启的文本历史也不会随 Android App 卸载删除，应在配套动作窗口中清空。
