# 隐私说明

Quicker Link 不提供账号系统，不包含广告、分析、崩溃上报或第三方统计 SDK。

## 本地保存的数据

App 会在设备本地保存：

- 电脑局域网 IPv4 和端口
- 用户创建的动作快捷项及其参数
- 已同步动作的名称、分组、顺序和图标描述
- 用户选择“加密保存验证码”时的连接验证码

连接验证码使用 Android Keystore 中不可导出的 AES 密钥加密。为避免凭据或动作配置随系统迁移，App 关闭了 Android 自动备份，并在数据提取规则中对云备份和设备迁移显式排除共享偏好、文件、数据库、外部文件及设备保护存储域。

## 网络通信

App 的动作执行和目录同步只通过 `WSS` 连接局域网内的 Quicker WebSocket 服务，不提供明文 `WS` 模式。

自动发现仅探测当前 Wi-Fi 或以太网私有 IPv4 网段中的配置端口，探测请求不包含连接验证码。完整扫描只发现一个 WSS 候选时，App 才与该地址建立正常连接并发送验证码进行 Quicker 身份验证；发现多个候选时要求改用配对码或手动地址。

扫描配对码时，相机画面只在设备内存中交给 ZXing 解析，不保存照片，也不上传画面或二维码内容。

App 不会把动作、参数、验证码或剪贴板内容发送给项目维护者或其他云端服务。

展示动作网格时，App 可能从 Quicker 官方静态文件域名 `files.getquicker.net` 加载动作设置的 PNG 图标。客户端严格限制为该域名下的图标和系统图片路径，不接受任意第三方图标地址；请求不会附带连接验证码、动作参数、剪贴板内容或局域网地址。Quicker 字体图标由电脑端在本地转换为 PNG 数据，不产生图标网络请求。

只有用户在“关于”页面主动点击“手动检查更新”时，App 才会向 GitHub Releases 的公开接口查询版本与发布文件元数据。发现新版本后，只有用户继续点击“下载并安装”，App 才会从该 Release 下载 APK 与 SHA-256 文件，在本地完成校验后交给 Android 系统安装器。请求不包含设备标识、Quicker 数据或连接信息；App 不会在后台自动发起检查、下载或安装。点击项目、作者、支持、反馈或配套动作入口时，将由系统浏览器打开对应网页。

## 删除数据

删除 App 会一并删除其本地配置、动作快捷项和加密密钥。
